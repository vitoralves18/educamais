# Copie este arquivo para .env.production (ou configure a mesma variável
# diretamente no painel do Vercel: Settings > Environment Variables)
# depois de ter a URL do backend publicado no Render.
VITE_API_URL=https://SEU-BACKEND.onrender.com/api
